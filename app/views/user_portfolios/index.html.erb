<div class="container">
  <table class="table table-striped table-sm __web-inspector-hide-shortcut__">
    <thead>
    <tr>
      <th>投資対象</th>
      <th>投資合計額</th>
      <th>現在合計額</th>
      <th>損益額</th>
      <th>積立月額</th>
      <th>最終更新日</th>
    </tr>
    </thead>
    <% @user_portfolio.each do |portfolio| %>
      <tbody>
      <tr>
        <td>
          <%= Investment.find_by(id: portfolio.investment_id).name %>
        </td>
        <td>
          <%= number_with_delimiter(portfolio.total_value) %>
        </td>
        <td>
          0
        </td>
        <td>
          0
        </td>

        <td>
          <%= number_with_delimiter(portfolio.reserve_value) %>
        </td>
        <td> <%= p portfolio.updated_at.strftime("%Y-%m-%d(%a)") %>
        </td>
      </tr>
      </tbody>
    <% end %>
  </table>
</div>
